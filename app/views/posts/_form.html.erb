<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field" 
   <%= @profile_id %>
     <% form.hidden_field :profile_id %>
     <%= form.label :profile_id %>
    <% form.text_field :profile_id, id: :post_profile_id %>
  </div> 

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_area :subject, id: :post_subject %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :post_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
