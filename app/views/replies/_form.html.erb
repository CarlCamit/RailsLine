<%= form_with(model: [@post, @post.replies.build]) do |form| %>
  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :reply_body %>
  </div>

  <div class="actions">
    <%= form.submit 'Reply' %>
  </div>
<% end %>
